BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Evaluation] ADD [scoreQuality] INT,
[scoreSpeed] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
