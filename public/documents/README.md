# Form thumbnail images

Drop an image named after the form code here to replace its icon tile on
`/documents` — e.g. `F06.png`, `F11.jpg`, `ITR.svg`. Supported extensions:
png, jpg, jpeg, webp, svg. No code changes needed; it picks up automatically
on next page load. Delete the file to fall back to the icon tile again.
